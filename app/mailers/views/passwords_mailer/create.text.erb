G'day <%= @user.username %>,

A password reset requested was made for <%= @user.username %>, <%= absolute_url(:links, @user.username) %>

You can alter your password at:
<%= absolute_url(:edit_password, @user.username, {:recover => @user.password_reset}) %>

Cheers,
The Oursignal Server

